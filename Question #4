# TODO: Importez vos modules ici
import math
from collections import Counter
import turtle


# TODO: Définissez vos fonction ici

#QUESTION 4:

#a)
def valide(saisie):
    if len(sequence_ADN) !=0:
        return True
        print('La séquence d'ADN est valide')
    else:
        return False
        print('La séquence d'ADN n'est pas valide')

#b)
def saisie(saisie):

    saisie=str(input('Veuillez saisir une séquence d'ADN:'))
    if len(saisie)!=0:
        return True
    else:
        return False

#c)
def proportion(chaine,sequence):
    count=0
    for sequence in len(chaine):
        count+=1
        proportion= len(chaine)/count  *100
        return proportion

        print(f'Il y a {proportion} de {sequence} dans la chaine')